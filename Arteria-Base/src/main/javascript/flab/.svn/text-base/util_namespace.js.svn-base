//= compat
//= require <flab/Namespace>
/**
 * @namespace Namespace for small helper classes
 *
 * Note: This is not intended as a collection of all sort of 
 * "we-probably-could-use-it-sometime" stuff. Each and every
 * function here must be in use either in Arteria-Base or in
 * at least to other modules. Furthermore each function must
 * come along with a full set of unit tests.
 *
 * If this rules doesn't match, the function is removed.
 *
 * @name flab.util
 */
flab.Namespace.create("flab.util");
